const name = "Go",
    age = 1,
    gender = "female";

const sayHi = (n:string, a:number, g:string) => {
    console.log(`hi, ${n}, you are ${a} and a ${g}`);
}

sayHi(name, age, gender);

export { };

